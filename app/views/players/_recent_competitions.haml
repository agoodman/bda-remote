%section
  %h1 Recent Competitions
  %ul
    - @player.recent_competitions.each do |c|
      %li
        = link_to "#{c.name} ( #{c.vessels.where(player_id: @player.id).first.craft_url} )", competition_path(c)

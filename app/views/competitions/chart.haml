%section
  %h2 Stage Rankings
  %canvas#competition_chart
  %script{language: "JavaScript"}
    $(document).ready(function() {
    console.log("load chart.json for #{@competition.id}");
    $.getJSON("/competitions/#{@competition.id}/chart.json", loadVesselRanks);
    });
